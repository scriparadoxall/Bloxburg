local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")

-- Configurações padrão
if _G.MostrarBolinhas == nil then _G.MostrarBolinhas = false end
if _G.MostrarObjetosIntel == nil then _G.MostrarObjetosIntel = false end

-- Limpeza
if _G.BloxburgChef and typeof(_G.BloxburgChef.Stop) == "function" then pcall(function() _G.BloxburgChef.Stop() end) end
if _G.SmartDoor and typeof(_G.SmartDoor.Cancelar) == "function" then pcall(function() _G.SmartDoor.Cancelar() end) end
if _G.Intelligence and typeof(_G.Intelligence.Stop) == "function" then pcall(function() _G.Intelligence.Stop() end) end
task.wait(0.2) 

-- 1. CARREGAR SMART DOOR
local smartDoorURL = "https://raw.githubusercontent.com/scriparadoxall/Bloxburg/main/smart_door.lua?t=" .. tostring(tick())
local loadDoorFunc, errDoor = loadstring(game:HttpGet(smartDoorURL))
if loadDoorFunc then _G.SmartDoor = loadDoorFunc() else warn("ERRO NO SMART DOOR: " .. tostring(errDoor)) return end

-- 2. CARREGAR BLOXBURG FOOD
local backendURL = "https://raw.githubusercontent.com/scriparadoxall/Bloxburg/main/BloxburgFood?t=" .. tostring(tick())
local loadFunc, loadErr = loadstring(game:HttpGet(backendURL))
if loadFunc then loadFunc() else warn("ERRO NO CÓDIGO DA COMIDA: " .. tostring(loadErr)) return end

-- 3. CARREGAR INTELLIGENCE
local intelURL = "https://raw.githubusercontent.com/scriparadoxall/Bloxburg/main/skills/Intelligence?t=" .. tostring(tick())
local loadIntelFunc, intelErr = loadstring(game:HttpGet(intelURL))
if loadIntelFunc then _G.Intelligence = loadIntelFunc() else warn("ERRO NO MÓDULO INTELLIGENCE: " .. tostring(intelErr)) end

if not _G.BloxburgChef then return end

pcall(function() if CoreGui:FindFirstChild("Scriparadoxall") then CoreGui.Scriparadoxall:Destroy() end end)
pcall(function() if Players.LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("Scriparadoxall") then Players.LocalPlayer.PlayerGui.Scriparadoxall:Destroy() end end)
pcall(function() if CoreGui:FindFirstChild("ScriparadoxWarning") then CoreGui.ScriparadoxWarning:Destroy() end end)
pcall(function() if Players.LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("ScriparadoxWarning") then Players.LocalPlayer.PlayerGui.ScriparadoxWarning:Destroy() end end)

local hideWarning = false
pcall(function()
    if isfile and isfile("Scriparadoxall_Warning.txt") then
        if readfile("Scriparadoxall_Warning.txt") == "true" then hideWarning = true end
    end
end)

local function LoadMainHub()
    local ScriparadoxallUI = Instance.new("ScreenGui")
    ScriparadoxallUI.Name = "Scriparadoxall"
    ScriparadoxallUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    local successUI = pcall(function() ScriparadoxallUI.Parent = CoreGui end)
    if not successUI then ScriparadoxallUI.Parent = Players.LocalPlayer:WaitForChild("PlayerGui") end

    local MainFrame = Instance.new("Frame", ScriparadoxallUI)
    MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
    MainFrame.Position = UDim2.new(0.5, -275, 0.5, -150)
    MainFrame.Size = UDim2.new(0, 550, 0, 300)
    MainFrame.ClipsDescendants = true 
    MainFrame.Active = true
    Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

    local dragging, dragInput, dragStart, startPos
    MainFrame.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = true; dragStart = input.Position; startPos = MainFrame.Position end end)
    game:GetService("UserInputService").InputChanged:Connect(function(input) if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + (input.Position - dragStart).X, startPos.Y.Scale, startPos.Y.Offset + (input.Position - dragStart).Y) end end)
    MainFrame.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)

    local Title = Instance.new("TextLabel", MainFrame)
    Title.BackgroundTransparency = 1; Title.Size = UDim2.new(1, -40, 0, 40); Title.Position = UDim2.new(0, 15, 0, 0)
    Title.Font = Enum.Font.GothamBold; Title.Text = "Scriparadoxall Hub"; Title.TextColor3 = Color3.fromRGB(255, 255, 255); Title.TextSize = 18; Title.TextXAlignment = Enum.TextXAlignment.Left

    local PausedLabel = Instance.new("TextLabel", MainFrame)
    PausedLabel.BackgroundTransparency = 1; PausedLabel.Size = UDim2.new(0, 150, 0, 40); PausedLabel.Position = UDim2.new(0, 200, 0, 0)
    PausedLabel.Font = Enum.Font.GothamBold; PausedLabel.Text = ""; PausedLabel.TextColor3 = Color3.fromRGB(255, 80, 80); PausedLabel.TextSize = 14; PausedLabel.TextXAlignment = Enum.TextXAlignment.Left

    local Divider = Instance.new("Frame", MainFrame)
    Divider.BackgroundColor3 = Color3.fromRGB(60, 60, 60); Divider.BorderSizePixel = 0; Divider.Position = UDim2.new(0, 0, 0, 40); Divider.Size = UDim2.new(1, 0, 0, 1)

    local HideButton = Instance.new("TextButton", MainFrame)
    HideButton.BackgroundTransparency = 1; HideButton.Position = UDim2.new(1, -40, 0, 0); HideButton.Size = UDim2.new(0, 40, 0, 40)
    HideButton.Font = Enum.Font.GothamBold; HideButton.Text = "-"; HideButton.TextColor3 = Color3.fromRGB(255, 255, 255); HideButton.TextSize = 24

    local OpenButton = Instance.new("ImageButton", ScriparadoxallUI)
    OpenButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    OpenButton.Position = UDim2.new(0, 20, 0.5, -25)
    OpenButton.Size = UDim2.new(0, 50, 0, 50)
    OpenButton.Image = "rbxthumb://type=Asset&id=93903389594586&w=150&h=150"
    OpenButton.Visible = false
    Instance.new("UICorner", OpenButton).CornerRadius = UDim.new(1, 0)

    local tweenInfo = TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
    
    local function MinimizeUI()
        if not MainFrame.Visible then return end
        TweenService:Create(MainFrame, tweenInfo, {Size = UDim2.new(0, 550, 0, 0)}):Play()
        task.wait(0.4)
        MainFrame.Visible = false; OpenButton.Visible = true
        TweenService:Create(OpenButton, tweenInfo, {Size = UDim2.new(0, 50, 0, 50)}):Play()
    end
    
    HideButton.MouseButton1Click:Connect(MinimizeUI)

    local PararTudo
    
    OpenButton.MouseButton1Click:Connect(function()
        if PararTudo then PararTudo() end
        TweenService:Create(OpenButton, tweenInfo, {Size = UDim2.new(0, 0, 0, 0)}):Play()
        OpenButton.Visible = false; MainFrame.Visible = true; 
        TweenService:Create(MainFrame, tweenInfo, {Size = UDim2.new(0, 550, 0, 300)}):Play()
    end)

    local Sidebar = Instance.new("Frame", MainFrame)
    Sidebar.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    Sidebar.Position = UDim2.new(0, 0, 0, 41)
    Sidebar.Size = UDim2.new(0, 130, 1, -41)
    Sidebar.BorderSizePixel = 0
    local SidebarLayout = Instance.new("UIListLayout", Sidebar); SidebarLayout.SortOrder = Enum.SortOrder.LayoutOrder; SidebarLayout.Padding = UDim.new(0, 2)

    local PagesContainer = Instance.new("Frame", MainFrame)
    PagesContainer.BackgroundTransparency = 1
    PagesContainer.Position = UDim2.new(0, 135, 0, 45)
    PagesContainer.Size = UDim2.new(1, -140, 1, -50)

    local tabs = {"Skills", "Works", "Utils", "Automations", "Config"}
    local pageFrames = {}
    local tabButtons = {}

    for i, tabName in ipairs(tabs) do
        local TabBtn = Instance.new("TextButton", Sidebar)
        TabBtn.Size = UDim2.new(1, 0, 0, 35)
        TabBtn.BackgroundColor3 = i == 1 and Color3.fromRGB(40, 40, 40) or Color3.fromRGB(20, 20, 20)
        TabBtn.BorderSizePixel = 0
        TabBtn.Font = Enum.Font.GothamSemibold; TabBtn.Text = tabName; TabBtn.TextColor3 = Color3.fromRGB(220, 220, 220); TabBtn.TextSize = 13
        tabButtons[tabName] = TabBtn

        local Page = Instance.new("Frame", PagesContainer)
        Page.Size = UDim2.new(1, 0, 1, 0); Page.BackgroundTransparency = 1; Page.Visible = (i == 1)
        pageFrames[tabName] = Page

        TabBtn.MouseButton1Click:Connect(function()
            for name, p in pairs(pageFrames) do p.Visible = (name == tabName) end
            for name, btn in pairs(tabButtons) do btn.BackgroundColor3 = (name == tabName) and Color3.fromRGB(40, 40, 40) or Color3.fromRGB(20, 20, 20) end
        end)
    end

    -- Texto de Versão Beta na Sidebar (para não bugar)
    local BetaSpacer = Instance.new("Frame", Sidebar) -- Espaçador para empurrar pro fundo
    BetaSpacer.BackgroundTransparency = 1
    BetaSpacer.Size = UDim2.new(1, 0, 1, -180) -- Ajusta o espaço livre restante
    
    local BetaText = Instance.new("TextLabel", Sidebar)
    BetaText.BackgroundTransparency = 1
    BetaText.Size = UDim2.new(1, 0, 0, 40)
    BetaText.Font = Enum.Font.Gotham
    BetaText.Text = "Hub Beta version\nhá diversos bugs\nsendo corrigidos."
    BetaText.TextColor3 = Color3.fromRGB(130, 130, 130)
    BetaText.TextSize = 10
    BetaText.TextWrapped = true

    --------------------------
    -- SKILLS
    --------------------------
    local SkillsPage = pageFrames["Skills"]
    local SkillsScroll = Instance.new("ScrollingFrame", SkillsPage)
    SkillsScroll.Size = UDim2.new(1, 0, 1, 0); SkillsScroll.BackgroundTransparency = 1; SkillsScroll.ScrollBarThickness = 4; SkillsScroll.BorderSizePixel = 0
    local SkillsLayout = Instance.new("UIListLayout", SkillsScroll); SkillsLayout.SortOrder = Enum.SortOrder.LayoutOrder; SkillsLayout.Padding = UDim.new(0, 6)

    -- COOKING
    local CookingFrame = Instance.new("Frame", SkillsScroll); CookingFrame.Size = UDim2.new(1, -10, 0, 75); CookingFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25); Instance.new("UICorner", CookingFrame).CornerRadius = UDim.new(0, 6)
    local CookingTitle = Instance.new("TextLabel", CookingFrame); CookingTitle.BackgroundTransparency = 1; CookingTitle.Position = UDim2.new(0, 10, 0, 5); CookingTitle.Size = UDim2.new(1, -20, 0, 20); CookingTitle.Font = Enum.Font.GothamBold; CookingTitle.Text = "👨‍🍳 Cooking (Auto-Farm)"; CookingTitle.TextColor3 = Color3.fromRGB(255, 200, 100); CookingTitle.TextSize = 14; CookingTitle.TextXAlignment = Enum.TextXAlignment.Left

    local selectedFoodUI = "Hot Dogs"
    local FoodBtn = Instance.new("TextButton", CookingFrame); FoodBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40); FoodBtn.Position = UDim2.new(0, 10, 0, 35); FoodBtn.Size = UDim2.new(0, 150, 0, 30); FoodBtn.Font = Enum.Font.GothamSemibold; FoodBtn.Text = "🍕 " .. selectedFoodUI; FoodBtn.TextColor3 = Color3.fromRGB(255, 255, 255); FoodBtn.TextSize = 12; Instance.new("UICorner", FoodBtn).CornerRadius = UDim.new(0, 6)
    local FarmBtn = Instance.new("TextButton", CookingFrame); FarmBtn.BackgroundColor3 = Color3.fromRGB(40, 150, 40); FarmBtn.Position = UDim2.new(0, 170, 0, 35); FarmBtn.Size = UDim2.new(0, 120, 0, 30); FarmBtn.Font = Enum.Font.GothamBold; FarmBtn.Text = "▶ Iniciar Farm"; FarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255); FarmBtn.TextSize = 12; Instance.new("UICorner", FarmBtn).CornerRadius = UDim.new(0, 6)

    -- INTELLIGENCE
    local IntelFrame = Instance.new("Frame", SkillsScroll); IntelFrame.Size = UDim2.new(1, -10, 0, 75); IntelFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25); Instance.new("UICorner", IntelFrame).CornerRadius = UDim.new(0, 6)
    local IntelTitle = Instance.new("TextLabel", IntelFrame); IntelTitle.BackgroundTransparency = 1; IntelTitle.Position = UDim2.new(0, 10, 0, 5); IntelTitle.Size = UDim2.new(1, -20, 0, 20); IntelTitle.Font = Enum.Font.GothamBold; IntelTitle.Text = "🧠 Intelligence (Auto-Read)"; IntelTitle.TextColor3 = Color3.fromRGB(150, 150, 255); IntelTitle.TextSize = 14; IntelTitle.TextXAlignment = Enum.TextXAlignment.Left

    local IntelFarmBtn = Instance.new("TextButton", IntelFrame); IntelFarmBtn.BackgroundColor3 = Color3.fromRGB(40, 150, 40); IntelFarmBtn.Position = UDim2.new(0, 10, 0, 35); IntelFarmBtn.Size = UDim2.new(0, 280, 0, 30); IntelFarmBtn.Font = Enum.Font.GothamBold; IntelFarmBtn.Text = "▶ Iniciar Inteligência"; IntelFarmBtn.TextColor3 = Color3.fromRGB(255, 255, 255); IntelFarmBtn.TextSize = 12; Instance.new("UICorner", IntelFarmBtn).CornerRadius = UDim.new(0, 6)

    local isIntelActive = false
    IntelFarmBtn.MouseButton1Click:Connect(function()
        if isIntelActive then
            isIntelActive = false; IntelFarmBtn.Text = "▶ Iniciar Inteligência"; IntelFarmBtn.BackgroundColor3 = Color3.fromRGB(40, 150, 40)
            if _G.Intelligence then _G.Intelligence.Stop() end
        else
            isIntelActive = true; IntelFarmBtn.Text = "🛑 Parar Inteligência"; IntelFarmBtn.BackgroundColor3 = Color3.fromRGB(150, 40, 40)
            PausedLabel.Text = ""
            if _G.Intelligence then _G.Intelligence.Start() end
            MinimizeUI()
        end
    end)

    SkillsLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() SkillsScroll.CanvasSize = UDim2.new(0, 0, 0, SkillsLayout.AbsoluteContentSize.Y + 10) end)

    -- PAINEL DE COMIDAS
    local FoodListPanel = Instance.new("ScrollingFrame", SkillsPage); FoodListPanel.BackgroundColor3 = Color3.fromRGB(25, 25, 25); FoodListPanel.Position = UDim2.new(0, 10, 0, 70); FoodListPanel.Size = UDim2.new(0, 150, 0, 145); FoodListPanel.ScrollBarThickness = 4; FoodListPanel.ZIndex = 10; FoodListPanel.Visible = false; Instance.new("UICorner", FoodListPanel).CornerRadius = UDim.new(0, 6)
    local FoodLayout = Instance.new("UIListLayout", FoodListPanel); FoodLayout.Padding = UDim.new(0, 2)
    local listaComidas = {"Baby Food", "Garden Salad", "Hot Dogs", "Corn on the Cob", "Vegetable Soup", "Fruit Salad", "Mini Corndogs"}
    for _, comida in ipairs(listaComidas) do
        local btn = Instance.new("TextButton", FoodListPanel); btn.BackgroundColor3 = Color3.fromRGB(35, 35, 35); btn.Size = UDim2.new(1, 0, 0, 30); btn.ZIndex = 11; btn.Font = Enum.Font.Gotham; btn.Text = comida; btn.TextColor3 = Color3.fromRGB(200, 200, 200); btn.TextSize = 12
        btn.MouseButton1Click:Connect(function() selectedFoodUI = comida; FoodBtn.Text = "🍕 " .. selectedFoodUI; FoodListPanel.Visible = false end)
    end
    FoodLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function() FoodListPanel.CanvasSize = UDim2.new(0, 0, 0, FoodLayout.AbsoluteContentSize.Y) end)
    FoodBtn.MouseButton1Click:Connect(function() FoodListPanel.Visible = not FoodListPanel.Visible end)

    local isUIActive = false
    FarmBtn.MouseButton1Click:Connect(function()
        if isUIActive then
            isUIActive = false; FarmBtn.Text = "▶ Iniciar Farm"; FarmBtn.BackgroundColor3 = Color3.fromRGB(40, 150, 40); if _G.BloxburgChef then _G.BloxburgChef.Stop() end
        else
            isUIActive = true; FarmBtn.Text = "🛑 Parar Farm"; FarmBtn.BackgroundColor3 = Color3.fromRGB(150, 40, 40); FoodListPanel.Visible = false; 
            PausedLabel.Text = "" 
            if _G.BloxburgChef then _G.BloxburgChef.Start(selectedFoodUI) end
            MinimizeUI()
        end
    end)
    
    PararTudo = function()
        local interrompeuAlgo = false
        if isIntelActive then
            isIntelActive = false
            IntelFarmBtn.Text = "▶ Retomar Inteligência"
            IntelFarmBtn.BackgroundColor3 = Color3.fromRGB(40, 150, 40)
            if _G.Intelligence then _G.Intelligence.Stop() end
            interrompeuAlgo = true
        end
        if isUIActive then
            isUIActive = false
            FarmBtn.Text = "▶ Retomar Farm"
            FarmBtn.BackgroundColor3 = Color3.fromRGB(40, 150, 40)
            if _G.BloxburgChef then _G.BloxburgChef.Stop() end
            interrompeuAlgo = true
        end
        if interrompeuAlgo then PausedLabel.Text = "⏸️ PAUSADO" end
    end

    --------------------------
    -- AUTOMATIONS (Logs)
    --------------------------
    local AutoPage = pageFrames["Automations"]
    local LogPanel = Instance.new("ScrollingFrame", AutoPage); LogPanel.BackgroundColor3 = Color3.fromRGB(12, 12, 12); LogPanel.Position = UDim2.new(0, 5, 0, 5); LogPanel.Size = UDim2.new(1, -15, 1, -10); LogPanel.ScrollBarThickness = 4; LogPanel.BorderSizePixel = 0; Instance.new("UICorner", LogPanel).CornerRadius = UDim.new(0, 6)
    local LogLayout = Instance.new("UIListLayout", LogPanel); LogLayout.SortOrder = Enum.SortOrder.LayoutOrder; LogLayout.Padding = UDim.new(0, 2)

    local logCount = 0
    _G.BloxburgChef_AddLog = function(texto, cor)
        logCount = logCount + 1
        local newLog = Instance.new("TextLabel", LogPanel); newLog.BackgroundTransparency = 1; newLog.Size = UDim2.new(1, -10, 0, 16); newLog.Font = Enum.Font.Code; newLog.Text = "[" .. os.date("%H:%M") .. "] " .. texto; newLog.TextColor3 = cor or Color3.fromRGB(200, 200, 200); newLog.TextSize = 11; newLog.TextXAlignment = Enum.TextXAlignment.Left; newLog.LayoutOrder = logCount
        LogPanel.CanvasSize = UDim2.new(0, 0, 0, LogLayout.AbsoluteContentSize.Y); LogPanel.CanvasPosition = Vector2.new(0, LogLayout.AbsoluteContentSize.Y)
    end
    _G.BloxburgChef_AddLog("Hub iniciado com sucesso!", Color3.fromRGB(50, 255, 255))

    --------------------------
    -- CONFIG
    --------------------------
    local ConfigPage = pageFrames["Config"]

    -- Anti-AFK (ATIVADO POR PADRÃO)
    local AntiAfkBtn = Instance.new("TextButton", ConfigPage); AntiAfkBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40); AntiAfkBtn.Position = UDim2.new(0, 10, 0, 10); AntiAfkBtn.Size = UDim2.new(0, 200, 0, 40); AntiAfkBtn.Font = Enum.Font.GothamSemibold; AntiAfkBtn.Text = "🛡️ Anti-AFK [ON]"; AntiAfkBtn.TextColor3 = Color3.fromRGB(100, 255, 100); AntiAfkBtn.TextSize = 14; Instance.new("UICorner", AntiAfkBtn).CornerRadius = UDim.new(0, 6)
    
    local antiAfkConnection; local isAntiAfkOn = true
    -- Inicia o anti-afk imediatamente
    antiAfkConnection = Players.LocalPlayer.Idled:Connect(function() VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame) task.wait(1) VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame) end)

    AntiAfkBtn.MouseButton1Click:Connect(function()
        isAntiAfkOn = not isAntiAfkOn
        if isAntiAfkOn then
            AntiAfkBtn.Text = "🛡️ Anti-AFK [ON]"; AntiAfkBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
            antiAfkConnection = Players.LocalPlayer.Idled:Connect(function() VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame) task.wait(1) VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame) end)
        else
            AntiAfkBtn.Text = "🛡️ Anti-AFK [OFF]"; AntiAfkBtn.TextColor3 = Color3.fromRGB(255, 100, 100); if antiAfkConnection then antiAfkConnection:Disconnect() end
        end
    end)

    -- Rota Visual
    local RouteBtn = Instance.new("TextButton", ConfigPage); RouteBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40); RouteBtn.Position = UDim2.new(0, 10, 0, 60); RouteBtn.Size = UDim2.new(0, 200, 0, 40); RouteBtn.Font = Enum.Font.GothamSemibold; RouteBtn.TextSize = 14; Instance.new("UICorner", RouteBtn).CornerRadius = UDim.new(0, 6)
    if _G.MostrarBolinhas then RouteBtn.Text = "👁️ Rota Visual [ON]"; RouteBtn.TextColor3 = Color3.fromRGB(100, 255, 100) else RouteBtn.Text = "👁️ Rota Visual [OFF]"; RouteBtn.TextColor3 = Color3.fromRGB(255, 100, 100) end
    RouteBtn.MouseButton1Click:Connect(function()
        _G.MostrarBolinhas = not _G.MostrarBolinhas
        if _G.MostrarBolinhas then RouteBtn.Text = "👁️ Rota Visual [ON]"; RouteBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
        else RouteBtn.Text = "👁️ Rota Visual [OFF]"; RouteBtn.TextColor3 = Color3.fromRGB(255, 100, 100) end
    end)

    -- Mostrar Objetos
    local ObjBtn = Instance.new("TextButton", ConfigPage); ObjBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40); ObjBtn.Position = UDim2.new(0, 10, 0, 110); ObjBtn.Size = UDim2.new(0, 200, 0, 40); ObjBtn.Font = Enum.Font.GothamSemibold; ObjBtn.TextSize = 13; Instance.new("UICorner", ObjBtn).CornerRadius = UDim.new(0, 6)
    if _G.MostrarObjetosIntel then ObjBtn.Text = "📦 Marcar Objetos [ON]"; ObjBtn.TextColor3 = Color3.fromRGB(100, 255, 100) else ObjBtn.Text = "📦 Marcar Objetos [OFF]"; ObjBtn.TextColor3 = Color3.fromRGB(255, 100, 100) end
    ObjBtn.MouseButton1Click:Connect(function()
        _G.MostrarObjetosIntel = not _G.MostrarObjetosIntel
        if _G.MostrarObjetosIntel then ObjBtn.Text = "📦 Marcar Objetos [ON]"; ObjBtn.TextColor3 = Color3.fromRGB(100, 255, 100)
        else ObjBtn.Text = "📦 Marcar Objetos [OFF]"; ObjBtn.TextColor3 = Color3.fromRGB(255, 100, 100) end
    end)
end

if hideWarning then LoadMainHub() else
    local WarningUI = Instance.new("ScreenGui"); WarningUI.Name = "ScriparadoxWarning"; WarningUI.ZIndexBehavior = Enum.ZIndexBehavior.Sibling; pcall(function() WarningUI.Parent = CoreGui end); if not WarningUI.Parent then WarningUI.Parent = Players.LocalPlayer:WaitForChild("PlayerGui") end
    local WarnFrame = Instance.new("Frame", WarningUI); WarnFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20); WarnFrame.Position = UDim2.new(0.5, -225, 0.5, -125); WarnFrame.Size = UDim2.new(0, 450, 0, 250); Instance.new("UICorner", WarnFrame).CornerRadius = UDim.new(0, 8)
    local WarnTitle = Instance.new("TextLabel", WarnFrame); WarnTitle.BackgroundTransparency = 1; WarnTitle.Size = UDim2.new(1, 0, 0, 40); WarnTitle.Font = Enum.Font.GothamBold; WarnTitle.Text = "⚠️ IMPORTANT WARNING ⚠️"; WarnTitle.TextColor3 = Color3.fromRGB(255, 80, 80); WarnTitle.TextSize = 18
    local ContinueBtn = Instance.new("TextButton", WarnFrame); ContinueBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 200); ContinueBtn.Position = UDim2.new(1, -120, 1, -65); ContinueBtn.Size = UDim2.new(0, 100, 0, 30); ContinueBtn.Font = Enum.Font.GothamBold; ContinueBtn.Text = "I Understand"; ContinueBtn.TextColor3 = Color3.fromRGB(255, 255, 255); ContinueBtn.TextSize = 14; Instance.new("UICorner", ContinueBtn).CornerRadius = UDim.new(0, 6)
    ContinueBtn.MouseButton1Click:Connect(function() WarningUI:Destroy() LoadMainHub() end)
end
